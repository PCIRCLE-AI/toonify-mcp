{
  "description": "Toonify MCP - Automatic token optimization hook for structured data",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "node ${CLAUDE_PLUGIN_ROOT}/hooks/post-tool-use.js",
            "timeout": 15
          }
        ]
      }
    ]
  }
}
